import { notFound } from "next/navigation";import { isLocale } from "@/lib/i18n";import { InnerPage } from "@/components/inner-page";import { Button } from "@/components/ui/button";
export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();return <InnerPage locale={locale} eyebrow="ONBOARDING" title="Configurez votre réceptionniste IA." text="Ce parcours servira à recueillir les informations nécessaires à votre agent vocal."><form className="grid gap-5 rounded-3xl border border-white/10 p-8">{["Nom de l’entreprise","Secteur d’activité","Horaires d’ouverture","Numéro à connecter","Lien Google Calendar"].map(x=><label key={x} className="grid gap-2 text-sm text-zinc-400">{x}<input className="h-12 rounded-2xl border border-white/10 bg-white/[.03] px-4 outline-none focus:border-gold-300/60"/></label>)}<Button type="button">Enregistrer la configuration</Button></form></InnerPage>}
