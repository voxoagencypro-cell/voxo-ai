import { notFound } from "next/navigation";import { isLocale } from "@/lib/i18n";import { InnerPage } from "@/components/inner-page";import { Button } from "@/components/ui/button";
export default async function Page({params,searchParams}:{params:Promise<{locale:string}>;searchParams:Promise<{plan?:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const{plan="pro"}=await searchParams;const price=plan==="business"?"449":"249";return <InnerPage locale={locale} eyebrow="CHECKOUT" title={`Offre ${plan}`} text="Écran prêt pour la future intégration Stripe Checkout."><div className="mx-auto max-w-xl rounded-3xl border border-white/10 bg-white/[.025] p-8"><div className="flex justify-between border-b border-white/10 pb-6"><span className="capitalize">VOXO AI {plan}</span><strong>{price} CHF/mois</strong></div><p className="py-7 leading-7 text-zinc-500">Aucun paiement réel n’est effectué dans cette version. Le bouton sera relié à Stripe lors de la configuration commerciale.</p><Button href={`/${locale}/onboarding?plan=${plan}`} className="w-full">Continuer vers l’onboarding</Button></div></InnerPage>}
